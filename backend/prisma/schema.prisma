generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
}

model Post {
  id       Int      @id @default(autoincrement())
  title    String
  content  String?
  createAt DateTime @default(now())

  @@map("Post")
}

model Blogs {
  id             Int      @id @default(autoincrement())
  title          String
  images         String[]
  posteddate     String
  postedby       String
  blogornews     String
  infoblogornews String

  @@map("Blogs")
}

model Gallery {
  id       Int      @id @default(autoincrement())
  database String[]
  slides   String[]

  @@map("Gallery")
}

model Events {
  id                   Int    @id @default(autoincrement())
  urlimage             String
  title                String
  description          String
  schedulestartinfo    String
  distants             String
  route                String
  registrationearly    String
  registrationstandart String
  linkforregistration  String
  recommendedequipment String
  mandatoryequipment   String
  contacts             String

  @@map("Events")
}

model Results {
  id             Int    @id @default(autoincrement())
  urlimage       String
  urllinkresults String

  @@map("Results")
}

model Contact {
  id       Int    @id @default(autoincrement())
  name     String
  lastname String
  topic    String
  message  String

  @@map("Contact")
}
